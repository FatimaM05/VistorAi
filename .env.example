# Postgres connection string (Neon, Supabase, Vercel Postgres, etc.)
DATABASE_URL="postgresql://user:password@host:5432/azaisai?sslmode=require"

# NextAuth
NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"
NEXTAUTH_URL="http://localhost:3000"

# Image generation (required for real image output)
OPENAI_API_KEY="sk-..."

# Video generation (optional — falls back to a queued/demo state if absent)
REPLICATE_API_TOKEN="r8_..."
